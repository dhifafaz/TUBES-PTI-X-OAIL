import React from 'react';
import {
    SafeAreaView,
    Text,
    View,
    ActivityIndicator,
    FlatList,
    ScrollView
} from 'react-native';
import { Image, Icon } from 'react-native-elements';
import styles from '../style/peminjamStyle';

const RiwayatPage = ({ navigation }) => {
    return (
        <View>
            <Text>ini RiwayatPage</Text>
        </View>
    )
}

export default RiwayatPage;