import * as React from 'react';
import {
    SafeAreaView,
    Text,
    View,
    ActivityIndicator,
    FlatList,
} from 'react-native';

const ProfilPage = ({ navigation }) => {

    return (
        <View>
            <Text>
                Ini ProfilPage
            </Text>
        </View>
    )

}

export default ProfilPage;